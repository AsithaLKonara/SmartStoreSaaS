services:
  mongodb:
    image: mongo:latest
    container_name: smartstore-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: smartstore
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    networks:
      - smartstore-network

  redis:
    image: redis:7.2-alpine
    container_name: smartstore-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - smartstore-network

  mongo-express:
    image: mongo-express:1.0.0
    container_name: smartstore-mongo-express
    restart: unless-stopped
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: admin
      ME_CONFIG_MONGODB_ADMINPASSWORD: password
      ME_CONFIG_MONGODB_URL: mongodb://admin:password@mongodb:27017/
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: password
    ports:
      - "8081:8081"
    depends_on:
      - mongodb
    networks:
      - smartstore-network

  app:
    build: .
    container_name: smartstore-app
    restart: unless-stopped
    environment:
      - DATABASE_URL=mongodb://admin:password@mongodb:27017/smartstore
      - REDIS_URL=redis://redis:6379
      - NEXTAUTH_SECRET=smartstore-secret-key-2024
      - NEXTAUTH_URL=http://localhost:3000
      - NODE_ENV=production
      - NEXT_PUBLIC_APP_URL=http://localhost:3000
    ports:
      - "3000:3000"
    depends_on:
      - mongodb
      - redis
    networks:
      - smartstore-network

volumes:
  mongodb_data:
    driver: local
  redis_data:
    driver: local

networks:
  smartstore-network:
    driver: bridge 